# 07. Shipping the branch

Status: not started. Branch feat/acp-agent-harness is local-only.

## State
- ~26 commits on top of main: Louis's ACP work rebuilt as a reviewable series (13 commits, co-authored to Louis), then icons/naming/UX, model+mode selection end to end, adapter probe, and runtime bug fixes. History is clean and each commit message carries full context.
- Unstaged, deliberately uncommitted: src-tauri/Cargo.lock (version-string churn) and gen/schemas/capabilities.json (regenerated by local builds). Commit only if CI requires them; user rule: never commit incidental generated churn.
- Untracked: .research/ (analysis, this roadmap), external/ (cloned ACP org repos for verification). Never commit either.

## Before PR
1. Full local gates: bun run typecheck, bunx vitest run, bun test, cargo test -p screenpipe-app (acp_runtime, pi_command_queue, store), bun run bindings:check.
2. Visual pass in the dev app (user does this): preset editors, probe loading state, composer dropdowns, subagent grouping once merged.
3. CI runs the desktop e2e suite (acp-backend.spec.ts, 9 tests + coverage-map check); watch the first run.

## PR composition
- Consider two PRs: (a) the rebuilt ACP harness series (Louis's work + fixes), (b) the model/mode/preset UX on top. CEO context suggests one PR may be fine since the branch tells the story commit by commit.
- PR body: bullet points, single section, no em dashes, no Generated-with footer (user rules). Visuals required by CLAUDE.md: screenshots of preset editor with agent icons + model/mode pickers, composer dropdowns, tool call details; screen recording of probe flow if possible.
- Note in the body which claims are source-verified (adapter capability matrix) and the deliberate constraints (cloud JWT ban, pipes stay raw-pi).
